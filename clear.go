package main

func (p *plugin) clear() (undo bool, err error) {
	/*var enable bool
	if enable, err = strconv.ParseBool(conf.Clear); nil != err || !enable {
		return
	}*/

	// 清理存储桶
	// client.Bucket.Get()

	return
}
